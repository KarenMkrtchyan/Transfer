specVersion: "v1beta"
serviceId: "Transfer"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "Transfer-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
